[project]
name = "reveng-workspace-build"
version = "0.1.0"
description = "Workspace build orchestration CLI"
requires-python = ">=3.12"
dependencies = [
    "reveng-types",
    "reveng-errors",
    "reveng-logging",
    "reveng-config",
    "reveng-codegen",
]

[project.scripts]
reveng-workspace-build = "reveng_workspace_build.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"


[tool.uv.sources]
reveng-types = { workspace = true }
reveng-errors = { workspace = true }
reveng-logging = { workspace = true }
reveng-config = { workspace = true }
reveng-codegen = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["src/reveng_workspace_build"]
