# Core runtime dependencies — required to launch the app and run the
# core static-analysis path (load -> disassemble -> view -> security audit).
# Tested against Python 3.11 / 3.12 (see pyproject.toml). Python 3.13+/3.14
# is not yet supported by all of these wheels.

# GUI
PyQt6>=6.5.0
pyqtgraph>=0.13.0

# Binary loading & disassembly
capstone>=5.0.0
lief>=0.14.0
pefile>=2023.2.7

# Crypto / key analysis helpers
pycryptodome>=3.18.0

# Analysis & visualization
numpy>=1.24.0
networkx>=3.0
pandas>=1.5.0
matplotlib>=3.7.0

# Networking / threat intel / AI client
requests>=2.28.0
openai>=1.0.0
