[project]
name = "reveng-types"
version = "0.1.0"
description = "Shared engineering typed envelopes, IDs, and enums"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/reveng_types"]
