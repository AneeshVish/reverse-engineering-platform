[project]
name = "reveng-logging"
version = "0.1.0"
description = "Structured JSON logging facade for engineering tools"
requires-python = ">=3.12"
dependencies = [
    "reveng-types",
    "reveng-errors",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"


[tool.uv.sources]
reveng-types = { workspace = true }
reveng-errors = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["src/reveng_logging"]
