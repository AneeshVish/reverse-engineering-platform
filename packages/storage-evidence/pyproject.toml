[project]
name = "reveng-storage-evidence"
version = "0.1.0"
description = "RevENG reserved package — ownership only"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/reveng_storage_evidence"]
