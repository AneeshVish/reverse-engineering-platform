[project]
name = "reveng-intermediate-representation"
version = "0.1.0"
description = "RevENG canonical intermediate representation — immutable program model (Impl Spec 004)"
requires-python = ">=3.12"
dependencies = [
    "reveng-core-substrate",
    "reveng-types",
    "reveng-errors",
    "reveng-logging",
    "reveng-config",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
reveng-core-substrate = { workspace = true }
reveng-types = { workspace = true }
reveng-errors = { workspace = true }
reveng-logging = { workspace = true }
reveng-config = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["src/reveng_intermediate_representation"]
